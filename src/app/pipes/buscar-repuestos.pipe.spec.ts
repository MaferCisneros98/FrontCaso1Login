import { BuscarRepuestosPipe } from './buscar-repuestos.pipe';

describe('BuscarRepuestosPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarRepuestosPipe();
    expect(pipe).toBeTruthy();
  });
});
