import { BuscarfacturaPipe } from './buscarfactura.pipe';

describe('BuscarfacturaPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarfacturaPipe();
    expect(pipe).toBeTruthy();
  });
});
